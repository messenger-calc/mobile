// Модуль для настройки всех обработчиков событий
const setupHandlers = (socket: any) => {
  // Подключение обработчиков для событий
//   socket.on('connect', () => {
//     console.log('Socket connected:', socket.id);
//   });

//   socket.on('disconnect', (reason: any) => {
//     console.warn('Socket disconnected:', reason);
//   });

//   // Пример других событий
//   socket.on('call', (data: any) => {
//     console.log('Received call event:', data);
//   });

//   socket.on('message', (data: any) => {
//     console.log('Received message:', data);
//   });

  // Добавляй здесь обработчики для других событий
};

export default setupHandlers;